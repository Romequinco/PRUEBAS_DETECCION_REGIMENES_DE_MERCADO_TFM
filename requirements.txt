# Núcleo de datos y cálculo
numpy>=1.26
pandas>=2.1
scipy>=1.11

# Descarga de datos
yfinance>=0.2.40
requests>=2.31          # descarga directa (data_loader: FRED CSV público / fallbacks)
# FRED se descarga vía CSV público (sin API key); fredapi opcional si se usa key
# fredapi>=0.5

# Detectores (lista aprobada en FASE 3 — 12 detectores, 7 familias)
scikit-learn>=1.4        # k-means, GMM, PCA, preprocessing (D3, D12)
hmmlearn>=0.3            # HMM gaussiano / init de HMM-t (D4, D8)
statsmodels>=0.14        # Markov-Switching (D5)
ruptures>=1.1            # change-point: PELT oráculo offline (D7)
arch>=6.0               # GJR-GARCH-t (D6); MS-GARCH (D11) usa numpy/scipy propio
jumpmodels>=0.1         # Statistical Jump Model, Nystrup 2020 (D9)
torch>=2.1              # autoencoder ligero -> GMM (D12)

# Visualización y notebooks
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.9           # scripts/builders/_build_*: construcción de notebooks
nbconvert>=7.0          # ejecución/verificación de notebooks (scripts/verify, re-runs)

# Utilidades
pyarrow>=15.0            # parquet para data/raw y data/processed
