# ── Núcleo de datos y cálculo ──────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0             # catalog.yaml / benchmark_spec.yaml
pyarrow>=15.0           # parquet para data/raw y data/processed

# ── Capa de datos v2 (src/ingest/) ─────────────────────────────────────────
yfinance>=0.2.40        # índices, VIX/MOVE, ETFs, FX, commodities
requests>=2.31          # descarga directa (OFR, github/datahub)
# FRED se descarga vía API con key (FRED_API_KEY en .env; ver .env.example)
openpyxl>=3.1           # xlsx: Goyal-Welch (Google Sheet), JST macrohistory
xlrd>=2.0               # xls antiguo: Shiller ie_data.xls
pandas-datareader>=0.10 # Ken French (fallback famafrench)

# ── Detectores (Capa 1 congelada + Fase D) — 12 detectores, 7 familias ─────
scikit-learn>=1.4        # k-means, GMM, PCA (D3, D12)
hmmlearn>=0.3            # HMM gaussiano / init de HMM-t (D4, D8)
statsmodels>=0.14        # Markov-Switching (D5)
ruptures>=1.1            # change-point: PELT oráculo offline (D7)
arch>=6.0               # GJR-GARCH-t (D6)
jumpmodels>=0.1         # Statistical Jump Model, Nystrup 2020 (D9)
torch>=2.1              # autoencoder ligero -> GMM (D12)

# ── Visualización y notebooks ──────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.9           # construcción/lectura de notebooks
nbconvert>=7.0          # ejecución de notebooks (00_descarga, 01_eda)
